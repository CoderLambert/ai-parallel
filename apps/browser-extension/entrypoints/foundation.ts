import { defineUnlistedScript } from "wxt/utils/define-unlisted-script";

// Temporary build anchor for the foundation wave. Runtime entrypoints remain
// sourced from the staged legacy assets until #22 migrates them explicitly.
export default defineUnlistedScript(() => {});
