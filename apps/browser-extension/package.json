{
  "name": "@ai-parallel/browser-extension",
  "private": true,
  "version": "1.0.1",
  "description": "Manifest V3 browser extension for sending one prompt to multiple AI web chats in parallel.",
  "scripts": {
    "check": "node --check service-worker.js && node --check popup.js && node --check content/runner.js"
  }
}
