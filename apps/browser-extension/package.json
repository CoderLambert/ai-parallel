{
  "name": "@ai-parallel/browser-extension",
  "private": true,
  "version": "1.1.1",
  "description": "Manifest V3 workspace for comparing AI web models in parallel.",
  "scripts": {
    "check": "node --check service-worker.js && node --check popup.js && node --check content/runner.js && node --check workspace/workspace.js"
  }
}
